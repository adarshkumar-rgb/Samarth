import Link from "next/link";
import { ArrowRight, MapPin } from "lucide-react";
import { AppShell } from "@/components/samarth/app-shell";
import { ProblemMap } from "@/components/samarth/problem-map";
import { problems, statistics } from "@/data/mock";

export default function Dashboard(){return <AppShell eyebrow="Jharkhand community network" title="The change you are helping move."><section className="border-y border-border bg-[#11110f] px-6 py-8 text-[#f7f7f4] md:px-8"><p className="eyebrow text-[#d0d0c8]">Good afternoon, Aarav</p><div className="mt-5 flex flex-wrap items-end justify-between gap-8"><h2 className="editorial max-w-2xl text-4xl leading-[.95] md:text-5xl">Twenty-six parents in Kanke are asking for a safer crossing.</h2><Link href="/problems/new" className="border border-[#f7f7f4] px-4 py-3 text-xs font-bold uppercase tracking-[.12em]">Raise a challenge <ArrowRight className="inline" size={14}/></Link></div></section><div className="mt-8 grid gap-10 xl:grid-cols-[1.1fr_.9fr]"><section><div className="flex items-end justify-between border-b border-border pb-4"><div><p className="eyebrow">Nearby challenges</p><h2 className="editorial mt-2 text-3xl">Jharkhand needs your voice.</h2></div><Link href="/map" className="text-xs font-bold uppercase tracking-[.12em] underline underline-offset-4">Open map</Link></div><div className="divide-y divide-border">{problems.slice(0,4).map((problem)=><Link key={problem.id} href={`/problems/${problem.slug}`} className="group flex gap-4 py-5"><span className="font-mono text-xs text-muted-foreground">{problem.id}</span><div className="min-w-0 flex-1"><h3 className="text-base font-bold group-hover:underline">{problem.title}</h3><p className="mt-2 flex items-center gap-1 text-xs text-muted-foreground"><MapPin size={13}/>{problem.location} · {problem.reports} reports</p></div><div className="text-right"><b className="font-mono text-sm">{problem.match}%</b><span className="eyebrow mt-1 block text-muted-foreground">match</span></div></Link>)}</div></section><section><div className="border-b border-border pb-4"><p className="eyebrow">Geographic pulse</p><h2 className="editorial mt-2 text-3xl">Problems near you.</h2></div><div className="mt-5"><ProblemMap compact/></div></section></div><section className="mt-10 grid border-y border-border sm:grid-cols-4">{statistics.map((stat)=><div key={stat.label} className="border-b border-border px-4 py-5 sm:border-b-0 sm:border-r last:border-r-0"><b className="editorial block text-3xl">{stat.value}</b><span className="eyebrow mt-2 block text-muted-foreground">{stat.label}</span></div>)}</section></AppShell>}
