"use client";
import { useState } from "react";
import { ArrowRight, Building2, Check, UsersRound } from "lucide-react";
import { AppShell } from "@/components/samarth/app-shell";
import { matches, problems } from "@/data/mock";

export default function Hei(){const [accepted,setAccepted]=useState(false);const primary=problems[0];return <AppShell eyebrow="Institution intelligence · Jharkhand" title="Your expertise is needed nearby."><section className="border-y border-border bg-[#11110f] p-7 text-[#f7f7f4] md:p-9"><div className="flex flex-wrap items-start justify-between gap-6"><div><p className="eyebrow text-[#d0d0c8]">Institution profile / 92% complete</p><h2 className="editorial mt-5 text-4xl">BIT Mesra Mobility Lab</h2><p className="mt-4 max-w-xl text-sm leading-6 text-[#d0d0c8]">Transport research and Ranchi civic partnerships are producing unusually strong local opportunities this month.</p></div><Building2 size={32}/></div><div className="mt-9 grid grid-cols-3 border-t border-[#55554f] pt-5"><Metric value="7" label="Open matches"/><Metric value="91%" label="Average fit"/><Metric value="3" label="Active pilots"/></div></section><div className="mt-10 grid gap-10 lg:grid-cols-[1.1fr_.9fr]"><section><p className="eyebrow">Highest opportunity</p><h2 className="editorial mt-3 text-4xl">{primary.title}</h2><p className="mt-3 text-sm text-muted-foreground">{primary.location} · {primary.reports} community reports</p><p className="mt-7 max-w-xl border-l-2 border-foreground pl-4 text-base leading-7">Your road-safety expertise, Ranchi civic partnership and available student team form a high-confidence response.</p><div className="mt-7 flex flex-wrap items-center gap-4"><b className="font-mono text-4xl">{primary.match}%</b><span className="eyebrow">Match confidence</span><button onClick={()=>setAccepted(true)} className="ml-auto bg-foreground px-4 py-3 text-xs font-bold uppercase tracking-[.12em] text-background">{accepted ? <><Check className="mr-1 inline" size={14}/> Challenge accepted</> : <>Accept challenge <ArrowRight className="ml-1 inline" size={14}/></>}</button></div></section><section className="border border-border p-6"><p className="eyebrow">Capability profile</p><div className="mt-7 space-y-5">{matches[0].capabilities.map(([label,score])=><div key={label}><div className="flex justify-between font-mono text-xs uppercase"><span>{label}</span><span>{score}%</span></div><div className="mt-2 h-px bg-border"><div className="h-px bg-foreground" style={{width:`${score}%`}}/></div></div>)}</div><p className="mt-8 flex items-center gap-2 text-sm text-muted-foreground"><UsersRound size={16}/>12 faculty · 45 student builders</p></section></div><section className="mt-10 border-t border-border pt-6"><p className="eyebrow">Matched challenges</p><div className="mt-4 divide-y divide-border">{problems.slice(0,3).map((problem)=><div key={problem.id} className="flex flex-wrap items-center gap-4 py-5"><span className="font-mono text-xs">{problem.id}</span><div className="min-w-48 flex-1"><b>{problem.title}</b><p className="mt-1 text-xs text-muted-foreground">{problem.location} · {problem.reports} reports</p></div><b className="font-mono">{problem.match}%</b><span className="eyebrow">match</span></div>)}</div></section></AppShell>}
function Metric({value,label}:{value:string;label:string}){return <div><b className="editorial block text-3xl">{value}</b><span className="eyebrow mt-2 block text-[#d0d0c8]">{label}</span></div>}
