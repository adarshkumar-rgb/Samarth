"use client";
import { useState } from "react";
import { Check, ChevronRight, MapPin } from "lucide-react";
import { AppShell } from "@/components/samarth/app-shell";
import { matches } from "@/data/mock";
export default function Matches(){const [connected,setConnected]=useState("");return <AppShell eyebrow="Explainable matching · Jharkhand" title="The right people are already close."><section className="border-y border-border py-5"><p className="font-mono text-xs uppercase tracking-wider">Challenge / 0248 · unsafe school crossing · Kanke, Ranchi</p><p className="mt-2 text-sm text-muted-foreground">We found three high-confidence teams. Every score is based on visible signals—not a black box.</p></section><div className="mt-8 grid gap-px border border-border bg-border lg:grid-cols-3">{matches.map((match)=><article key={match.name} className="bg-background p-6"><div className="flex items-start justify-between"><div><p className="eyebrow">{match.type}</p><h2 className="editorial mt-4 text-3xl leading-none">{match.name}</h2></div><b className="font-mono text-3xl">{match.score}%</b></div><p className="mt-6 border-l-2 border-foreground pl-3 text-sm leading-6 text-muted-foreground">{match.why}</p><div className="mt-8 space-y-3">{match.capabilities.map(([label,score])=><div key={label as string}><div className="flex justify-between font-mono text-[11px] uppercase"><span>{label}</span><span>{score}%</span></div><div className="mt-2 h-px bg-border"><div className="h-px bg-foreground" style={{width:`${score}%`}}/></div></div>)}</div><button onClick={()=>setConnected(match.name)} className="mt-8 w-full border border-foreground py-3 text-xs font-bold uppercase tracking-[.12em]">{connected===match.name?<>Connection requested <Check className="ml-1 inline" size={14}/></>:<>Review collaboration <ChevronRight className="ml-1 inline" size={14}/></>}</button></article>)}</div><section className="mt-8 grid gap-6 border-t border-border pt-8 md:grid-cols-3"><div><p className="eyebrow">01</p><h3 className="editorial mt-3 text-2xl">Capability overlap</h3><p className="mt-2 text-sm text-muted-foreground">Domains, labs and experience.</p></div><div><p className="eyebrow">02</p><h3 className="editorial mt-3 text-2xl">Geographic relevance</h3><p className="mt-2 text-sm text-muted-foreground"><MapPin className="mr-1 inline" size={14}/> Jharkhand delivery context and civic relationships.</p></div><div><p className="eyebrow">03</p><h3 className="editorial mt-3 text-2xl">Available capacity</h3><p className="mt-2 text-sm text-muted-foreground">People and resources ready to begin.</p></div></section></AppShell>}
